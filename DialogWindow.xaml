<Window x:Class="Dubovik_Zadanie4.DialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dubovik_Zadanie4"
        mc:Ignorable="d"
        Title="DialogWindow" Height="200" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="129" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <Grid Margin="20">
            <StackPanel VerticalAlignment="Top" Orientation="Horizontal">
                <TextBlock Text="Первая переменная = "/>
                <TextBox x:Name="textBoxA" Width="120"/>
            </StackPanel>
            <StackPanel VerticalAlignment="Bottom" Orientation="Horizontal">
                <TextBlock Text="Вторая переменная = "/>
                <TextBox x:Name="textBoxB" Width="120"/>
            </StackPanel>
        </Grid>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button x:Name="buttonInput" Content="Добавить" Height="20" Width="120" Margin="20, 0, 0, 0" Click="buttonInput_Click" />
        </StackPanel>
    </Grid>
</Window>
